# Project Guidelines

## Git Workflow

- Commit work frequently with clean, descriptive commit messages as you go — never leave work uncommitted.
- Push commits to GitHub (origin/main) so we always have a saved version and can revert if needed.
- Keep commits focused: one logical change per commit.
- Write commit messages that explain *why* a change was made, not just what changed.
